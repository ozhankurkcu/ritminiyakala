{
  "name": "ritminiyakala",
  "version": "1.0.0",
  "description": "Ritminiyakala monorepo — asıl uygulama apps/web (Next.js)",
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "start": "npm --prefix apps/web run start",
    "lint": "npm --prefix apps/web run lint",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "firebase": "^12.12.1"
  },
  "devDependencies": {
    "firebase-admin": "^14.0.0"
  }
}
