rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Placeholder — Sprint 1'de avatar upload için detaylandırılacak
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
